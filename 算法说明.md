为了满足你的要求（可识别性、分离性、全天区覆盖、量化标准），我提出了以下改进方案：

# 分层聚类策略：

骨架（Anchors）：仅使用肉眼清晰可见的亮星（如 Vmag < 5.5 或 6.0）来确定星座的“核心”和质心。这保证了星座是基于肉眼可观测特征构建的。
填充（Partitioning）：确定核心后，将剩余较暗的恒星（Vmag > 6.0）根据球面最近邻原则分配给最近的星座。这实现了对整个天球的无缝划分。
量化标准（The Metric）：

使用 球面轮廓系数 (Spherical Silhouette Score)。这是一个标准化的指标，范围 [-1, 1]。
对于每颗星，计算其与同簇其他星的平均距离 $a$ 和与最近邻簇所有星的平均距离 $b$。分数 $s = (b-a)/\max(a,b)$。
该指标同时最大化了簇内紧密度和簇间分离度。
可识别性增强（Visuals）：

在每个星座内部，计算 最小生成树 (Minimum Spanning Tree, MST)。
MST 会连接簇内恒星，形成总长度最短的连线。这在视觉上非常接近人类构建星座时的“连线”逻辑，比单纯的凸包（Convex Hull）更具可观测性。


# 改进逻辑的详细解释
为什么使用轮廓系数 (Silhouette Score)？

你之前的标准是 min_inter - mean_intra。这虽然直观，但没有归一化。如果两个簇离得很远但都很松散，或者两个簇很紧密但离得很近，旧指标很难权衡。
轮廓系数计算每一点的“归属感”。如果某颗星距离自己簇内的星很近（$a$ 小），且距离邻近簇的星很远（$b$ 大），分数就接近 1。
通过最大化平均轮廓系数，我们能找到最自然的分组数量 $k$，使得星座既紧凑又互不干扰。
为什么区分 Anchors (骨架) 和 Partitioning (分区)？

问题：如果把所有 6.5 等星都放入 K-Means，算法会被大量暗星“淹没”，导致星座中心偏向暗星密集的区域，而不是亮星构成的形状中心。
解决：我们只用较亮的星（Vmag <= 5.5）来“训练”模型。这些星是肉眼最容易看到的，它们构成了星座的“骨架”。
全覆盖：训练好质心后，我们将剩余的暗星（5.5 < Vmag <= 6.5）直接指派给最近的质心。这实际上是在球面上构建了 Voronoi 图，确保了天球的每一个区域都有归属，符合你“天球分区”的要求。
为什么使用最小生成树 (MST) 画线？

之前的凸包（Convex Hull）画出来的是一个包围圈，看起来像“领地”而不是“星座”。
MST 寻找连接所有点的最短路径。在视觉上，这往往能还原出类似“北斗七星”或“仙后座 W 形”这样的线条结构，极大地增强了可识别性。
球面几何的处理

代码中使用了 SphericalKMeans 类。标准的 sklearn.KMeans 使用欧几里得距离。虽然在单位球面上，欧氏距离越小意味着角度越小，但标准 K-Means 的质心更新步骤（计算算术平均值）会使质心落入球体内部。
本代码在更新质心后强制执行 normalize，确保质心始终在天球表面上，这对大跨度的星座划分尤为重要。

